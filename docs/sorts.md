## Bubble
looks at value at current index and swaps with higher indices to the right until highest value is farthest to the right

## Selection
passes through the array n times and finds the next lowest value on each pass and inserts it to the n farthest left index

## Insertion
compares each index to the values on its left and inserts it where it belongs among those values